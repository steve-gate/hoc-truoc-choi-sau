<Window x:Class="FocusLock.App.BubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="292" Height="132" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize" ShowActivated="False" Focusable="False">
    <Border x:Name="RootBorder" Background="#F7FFFFFF" CornerRadius="20" Padding="15" BorderBrush="#E4E9F2" BorderThickness="1">
        <Border.Effect><DropShadowEffect BlurRadius="22" ShadowDepth="6" Opacity="0.16"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <DockPanel>
                <Border x:Name="ModePill" Background="#EAF3FF" CornerRadius="999" Padding="9,4" DockPanel.Dock="Left">
                    <TextBlock x:Name="ModeText" Foreground="#2D7FF9" FontWeight="SemiBold" FontSize="12" Text="ĐANG TẬP TRUNG"/>
                </Border>
                <TextBlock DockPanel.Dock="Right" Text="⋮" Foreground="#A0A7B4" FontSize="17" HorizontalAlignment="Right"/>
            </DockPanel>
            <TextBlock x:Name="TimeText" Grid.Row="1" Foreground="#182033" FontWeight="Bold" FontSize="31" Margin="0,8,0,0" Text="00:00"/>
            <TextBlock x:Name="DetailText" Grid.Row="2" Foreground="#6C7485" FontSize="11.5" Margin="0,3,0,0" TextTrimming="CharacterEllipsis" Text="Sẵn sàng"/>
        </Grid>
    </Border>
</Window>
